@model List<Taigate.Data.Data.Entities.Components.DoganlarMobilya.FooterInvestorRelationsList>

@foreach (var item in Model)
{
    <li>
        <a class="list-item" href="@item.RedirectUrl">@item.Title</a>
    </li>
}